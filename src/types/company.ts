export interface Notice {
  id: string;
  title: string;
  message: string;
  author: string;
  createdAt: Date;
}

export interface CompanyDoc {
  id: string;
  name: string;
  link: string;
  author: string;
  createdAt: Date;
}